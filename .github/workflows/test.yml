name: PR tags checks test

on:
  push:
    tags:
      - '*.*.*'
      - '[0-9]+.[0-9]+.[0-9]+'
      - '[0-9]+.[0-9]+.[0-9]+rc[0-9]+'

jobs:

  dummy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ '3.x' ]

    steps:
      - uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: Generate file
        run: rm docs/test1.txt && echo test1 > docs/test1.txt

      - name: Commit files
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add docs
          git commit -m "Test the action"
          git status

      - name: Push changes
        uses: ad-m/github-push-action@b80395bd2271903200b27db6eebc7b6ee046202e
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          force_with_lease: true
          tags: true
